---
interface Props {
  size?: 'sm' | 'md' | 'lg' | 'xl' | '2xl'
}

const { size = 'md' } = Astro.props

const sizes = {
  sm: '1rem',
  md: '2rem',
  lg: '4rem',
  xl: '6rem',
  '2xl': '8rem'
}
---

<div
  aria-hidden="true"
  style={`height: ${sizes[size]}; width: 100%;`}>
</div>
