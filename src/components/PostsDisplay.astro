---
import PostExcerpt from '../components/PostExcerpt.astro';
import { sortPosts } from '../functions/posts.js';

let { posts } = Astro.props

posts = sortPosts(posts)
---

{
posts.map((post) => (
	<PostExcerpt {post} />
	<br>
))
}
